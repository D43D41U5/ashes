packages:
  - packages/*
allowBuilds:
  esbuild: true
  # Accélération native optionnelle de msgpackr (Colyseus) : le pur JS suffit et
  # évite une toolchain node-gyp/C++ dans le build et le Docker (résister à la complexité).
  msgpackr-extract: false
