{
  "name": "ashes",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.10.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "pnpm -r run check",
    "test": "node tools/suites.mjs",
    "lint": "eslint .",
    "dev": "pnpm --filter @ashes/client dev",
    "build": "pnpm --filter @ashes/client build",
    "scenario": "pnpm --filter @ashes/sim run test:scenario",
    "smoke:install": "PLAYWRIGHT_BROWSERS_PATH=0 playwright install chromium",
    "smoke": "PLAYWRIGHT_BROWSERS_PATH=0 node tools/smoke.mjs"
  },
  "devDependencies": {
    "eslint": "^9.30.0",
    "playwright": "^1.61.1",
    "tsx": "^4.23.1",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.35.0"
  }
}
